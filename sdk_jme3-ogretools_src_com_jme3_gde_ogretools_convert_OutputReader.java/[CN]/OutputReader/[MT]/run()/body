{
  try {
    String line;
    while ((line=in.readLine()) != null) {
      if (line.trim().length() > 0) {
        if (progress != null) {
          progress.progress(line);
        }
 else {
          System.out.println(line);
        }
      }
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
