{
  if (x != vpX || vpY != y || vpW != w || vpH != h) {
    if (verboseLogging)     logger.info("GLES20.glViewport(" + x + ", "+ y+ ", "+ w+ ", "+ h+ ")");
    GLES20.glViewport(x,y,w,h);
    checkGLError();
    vpX=x;
    vpY=y;
    vpW=w;
    vpH=h;
  }
}
