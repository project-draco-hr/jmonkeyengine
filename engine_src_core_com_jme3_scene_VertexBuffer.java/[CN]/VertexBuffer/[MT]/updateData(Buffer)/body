{
  if (id != -1) {
  }
  if (this.data.getClass() != data.getClass() || data.limit() != lastLimit) {
    dataSizeChanged=true;
    lastLimit=data.limit();
  }
  this.data=data;
  setUpdateNeeded();
}
