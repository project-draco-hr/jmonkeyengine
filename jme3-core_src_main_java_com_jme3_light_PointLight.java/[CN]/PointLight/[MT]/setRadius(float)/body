{
  if (radius < 0) {
    throw new IllegalArgumentException("Light radius cannot be negative");
  }
  this.radius=radius;
  if (radius != 0f) {
    this.invRadius=1f / radius;
  }
 else {
    this.invRadius=0;
  }
}
