{
  deleteUnused(r);
  for (  GLObjectRef ref : refList) {
    ref.objClone.deleteObject(r);
    GLObject realObj=ref.realObj.get();
    if (realObj != null) {
      realObj.resetObject();
    }
  }
  refList.clear();
}
