{
  if (!pressed) {
    setDefaultAxisMarkerColors();
    pickedPlane=null;
    offset=null;
    if (wasDragging) {
      actionPerformed(new MoveUndo(selectedSpatial,startLoc,lastLoc));
      wasDragging=false;
    }
  }
}
