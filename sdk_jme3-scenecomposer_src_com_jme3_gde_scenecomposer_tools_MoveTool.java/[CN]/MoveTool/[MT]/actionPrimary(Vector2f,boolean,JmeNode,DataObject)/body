{
  if (!pressed) {
    setDefaultAxisMarkerColors();
    pickedPlane=null;
    if (wasDragging) {
      actionPerformed(moveManager.makeUndo());
      wasDragging=false;
    }
    moveManager.reset();
  }
}
