{
  if (rootPath == null)   throw new NullPointerException();
  root=new File(rootPath);
  if (!root.isDirectory())   throw new IllegalArgumentException("Given root path \"" + root + "\" not a directory");
}
