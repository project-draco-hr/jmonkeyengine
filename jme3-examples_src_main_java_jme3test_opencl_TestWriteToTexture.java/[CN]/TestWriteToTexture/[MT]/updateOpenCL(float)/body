{
  texCL.acquireImageForSharingAsync(clQueue).release();
  kernel.Run1(clQueue,new com.jme3.opencl.Kernel.WorkSize(settings.getWidth(),settings.getHeight()),texCL,C,16).release();
  texCL.releaseImageForSharingAsync(clQueue).release();
}
