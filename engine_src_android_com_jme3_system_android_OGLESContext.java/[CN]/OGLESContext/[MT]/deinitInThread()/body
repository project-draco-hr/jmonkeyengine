{
  if (renderable.get()) {
    renderable.set(false);
    if (renderer != null)     renderer.cleanup();
    listener.destroy();
    listener=null;
    renderer=null;
    timer=null;
    logger.info("Display destroyed.");
    created.set(false);
  }
}
