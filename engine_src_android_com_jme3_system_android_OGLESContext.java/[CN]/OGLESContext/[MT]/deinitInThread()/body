{
  if (renderable.get()) {
    created.set(false);
    if (renderer != null)     renderer.cleanup();
    listener.destroy();
    listener=null;
    renderer=null;
    timer=null;
    logger.info("Display destroyed.");
    renderable.set(false);
  }
}
