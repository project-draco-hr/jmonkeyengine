{
  File binDir=new File(parent,path);
  for (  File file : binDir.listFiles()) {
    try {
      file.setExecutable(true,false);
    }
 catch (    Exception ex) {
      ex.printStackTrace();
    }
  }
}
