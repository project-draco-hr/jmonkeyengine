{
  if ("idxSearchSort".equals(propertyName) || "idxSearchNoHtml".equals(propertyName) || "idxSearchSplit".equals(propertyName)) {
    String stringvalue=Utils.valueFromObjectWrapper(value);
    addProperty(propertyName,stringvalue);
  }
}
