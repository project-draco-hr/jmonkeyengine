{
  onStop();
  time=0;
  playState=PlayState.Stopped;
  elapsedTimePause=0;
  if (listeners != null) {
    for (int i=0; i < listeners.size(); i++) {
      CinematicEventListener cel=listeners.get(i);
      cel.onStop(this);
    }
  }
}
