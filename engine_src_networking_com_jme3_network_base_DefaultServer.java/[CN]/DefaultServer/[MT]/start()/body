{
  if (isRunning)   throw new IllegalStateException("Server is already started.");
  reliable.initialize();
  if (fast != null) {
    fast.initialize();
  }
  reliableAdapter.start();
  if (fastAdapter != null) {
    fastAdapter.start();
  }
  isRunning=true;
}
