{
  if (!isRunning)   return;
  services.stop();
  for (  ConnectorAdapter ca : channels) {
    if (ca == null)     continue;
    ca.close();
  }
  connecting.countDown();
  fireDisconnected(info);
  isRunning=false;
  services.terminate();
}
