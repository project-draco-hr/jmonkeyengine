{
  if (mp.isPlaying()) {
    mp.seekTo(0);
    mp.stop();
  }
  for (  AudioSource src : musicPlaying.keySet()) {
    if (musicPlaying.get(src) == mp) {
      src.setStatus(Status.Stopped);
      break;
    }
  }
}
