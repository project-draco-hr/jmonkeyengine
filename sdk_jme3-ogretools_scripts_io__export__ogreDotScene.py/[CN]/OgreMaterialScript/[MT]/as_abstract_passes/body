def as_abstract_passes(self):
    r = []
    for (i, P) in enumerate(self.passes):
        head = ('abstract pass %s/PASS%s' % (self.name, i))
        r.append(((head + '\n') + P['body']))
    return r
