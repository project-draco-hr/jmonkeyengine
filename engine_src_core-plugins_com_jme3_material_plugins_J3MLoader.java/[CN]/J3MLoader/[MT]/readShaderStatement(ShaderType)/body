{
  String lang=readString(":");
  String word=scan.next();
  throwIfNequal(":",word);
  word=readString("[\n;(\\})]");
  if (type == ShaderType.Vertex)   vertName=word;
 else   if (type == ShaderType.Fragment)   fragName=word;
  shaderLang=lang;
}
