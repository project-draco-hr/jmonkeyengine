{
  if (skeleton != null) {
    skeleton.reset();
  }
  for (int i=0; i < channels.size(); i++) {
    channels.get(i).update(tpf);
  }
  if (skeleton != null) {
    skeleton.updateWorldVectors();
  }
}
