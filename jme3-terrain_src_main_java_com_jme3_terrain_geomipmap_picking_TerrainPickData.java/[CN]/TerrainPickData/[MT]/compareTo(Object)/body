{
  if (o instanceof TerrainPickData) {
    TerrainPickData tpd=(TerrainPickData)o;
    if (this.cr.getDistance() < tpd.cr.getDistance())     return -1;
 else     if (this.cr.getDistance() == tpd.cr.getDistance())     return 0;
 else     return 1;
  }
  return 0;
}
