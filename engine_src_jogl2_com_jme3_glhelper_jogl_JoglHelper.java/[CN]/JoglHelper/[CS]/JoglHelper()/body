{
  GL gl=GLContext.getCurrentGL();
  if (gl.isGL2ES1()) {
  }
 else {
    if (gl.isGL2ES2()) {
    }
 else {
      if (gl.isGL3() && !gl.isGL3bc() || gl.isGL4() && !gl.isGL4bc()) {
      }
 else {
      }
    }
  }
}
