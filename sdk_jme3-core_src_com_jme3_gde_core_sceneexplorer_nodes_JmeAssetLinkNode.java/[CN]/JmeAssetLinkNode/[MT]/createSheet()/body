{
  Sheet sheet=super.createSheet();
  Sheet.Set set=Sheet.createPropertiesSet();
  set.setDisplayName("AssetLinkNode");
  set.setName(AssetLinkNode.class.getName());
  AssetLinkNode obj=geom;
  if (obj == null) {
    return sheet;
  }
  sheet.put(set);
  return sheet;
}
