{
  Spatial exclude=null;
  if (excludeSpat != null)   exclude=excludeSpat.getLookup().lookup(Spatial.class);
  CollisionResult cr=doPick(cam,mouseLoc,rootNode,exclude);
  if (cr != null) {
    return cr.getContactPoint();
  }
 else {
    return null;
  }
}
