{
  int lay=((Number)structure.getFieldValue("lay")).intValue();
  return ((lay & dataRepository.getBlenderKey().getLayersToLoad()) != 0 && (dataRepository.getBlenderKey().getFeaturesToLoad() & FeaturesToLoad.OBJECTS) != 0);
}
