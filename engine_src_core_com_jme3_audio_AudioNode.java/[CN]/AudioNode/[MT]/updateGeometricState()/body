{
  boolean updatePos=false;
  if ((refreshFlags & RF_TRANSFORM) != 0) {
    updatePos=true;
  }
  super.updateGeometricState();
  if (updatePos && renderer != null)   renderer.updateSourceParam(this,AudioParam.Position);
}
