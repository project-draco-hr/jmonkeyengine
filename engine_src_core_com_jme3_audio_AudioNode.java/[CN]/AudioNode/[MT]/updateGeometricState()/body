{
  boolean updatePos=false;
  if ((refreshFlags & RF_TRANSFORM) != 0) {
    updatePos=true;
  }
  super.updateGeometricState();
  if (updatePos && channel >= 0)   renderer.updateSourceParam(this,AudioParam.Position);
}
