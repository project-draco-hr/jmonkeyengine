{
  if (maxDistance < 0) {
    throw new IllegalArgumentException("Max distance cannot be negative");
  }
  this.maxDistance=maxDistance;
  if (channel >= 0)   renderer.updateSourceParam(this,AudioParam.MaxDistance);
}
