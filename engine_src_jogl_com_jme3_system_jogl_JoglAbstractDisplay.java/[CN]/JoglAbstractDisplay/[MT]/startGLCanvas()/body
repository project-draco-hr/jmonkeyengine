{
  if (frameRate > 0) {
    animator=new FPSAnimator(canvas,frameRate);
  }
 else {
    animator=new Animator();
    animator.add(canvas);
  }
  animator.start();
  wasAnimating=true;
  renderable.set(true);
}
