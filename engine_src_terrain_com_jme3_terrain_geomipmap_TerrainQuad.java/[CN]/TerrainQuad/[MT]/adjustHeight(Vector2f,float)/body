{
  int x=Math.round((xz.x / getLocalScale().x) + totalSize / 2);
  int z=Math.round((xz.y / getLocalScale().z) + totalSize / 2);
  if (!isPointOnTerrain(x,z))   return;
  adjustHeight(x,z,delta);
  setNormalRecalcNeeded(xz);
}
