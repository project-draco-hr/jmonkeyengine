{
  int r=ringBuffer.read(buf,offset,length);
  if (r <= 0) {
    for (int i=0; i < length; i++) {
      buf[offset + i]=0x0;
    }
    return length;
  }
 else {
    lastPtsRead=System.nanoTime();
  }
  return r;
}
