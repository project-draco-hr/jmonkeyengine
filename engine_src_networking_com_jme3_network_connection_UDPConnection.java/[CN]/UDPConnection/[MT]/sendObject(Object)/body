{
  if (target == null) {
synchronized (connections) {
      for (      Client connector : connections) {
        send(connector.getDatagramReceiver(),object);
      }
    }
  }
 else {
    send(target,object);
  }
}
