{
  WizardDescriptor wiz=(WizardDescriptor)settings;
  SkyboxVisualPanel1 comp=(SkyboxVisualPanel1)getComponent();
  boolean multiple="Multiple textures".equalsIgnoreCase(comp.getSkyTypeCombo().getSelectedItem().toString());
  wiz.putProperty("multipleTextures",multiple);
}
