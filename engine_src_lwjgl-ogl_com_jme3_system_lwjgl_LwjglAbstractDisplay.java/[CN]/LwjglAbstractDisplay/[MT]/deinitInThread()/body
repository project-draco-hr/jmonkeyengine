{
  if (Display.isCreated()) {
    renderer.cleanup();
    Display.destroy();
  }
 else {
    renderer.resetGLObjects();
  }
  listener.destroy();
  logger.info("Display destroyed.");
  super.internalDestroy();
}
