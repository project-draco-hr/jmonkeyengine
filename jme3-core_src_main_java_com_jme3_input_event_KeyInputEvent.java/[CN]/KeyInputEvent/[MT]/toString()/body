{
  String str="Key(CODE=" + keyCode;
  if (keyChar != '\0')   str=str + ", CHAR=" + keyChar;
  if (repeating) {
    return str + ", REPEATING)";
  }
 else   if (pressed) {
    return str + ", PRESSED)";
  }
 else {
    return str + ", RELEASED)";
  }
}
