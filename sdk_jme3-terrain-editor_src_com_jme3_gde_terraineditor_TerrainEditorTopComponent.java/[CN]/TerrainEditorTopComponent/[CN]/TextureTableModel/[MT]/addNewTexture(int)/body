{
  float scale=TerrainEditorController.DEFAULT_TEXTURE_SCALE;
  addRow(new Object[]{"",newIndex,null,scale});
  setTextureScale(newIndex,scale);
  setTexture(newIndex,(String)null);
  editorController.enableTextureButtons();
}
