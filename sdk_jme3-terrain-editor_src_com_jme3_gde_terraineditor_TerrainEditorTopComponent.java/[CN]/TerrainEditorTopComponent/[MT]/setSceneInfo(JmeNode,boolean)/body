{
  final TerrainEditorTopComponent inst=this;
  if (jmeNode != null) {
  }
 else {
  }
  if (!active) {
    result.removeLookupListener(inst);
    close();
  }
 else {
    open();
    requestActive();
  }
}
