{
  if (editorController == null || editorController.getTerrain(null) == null)   return;
  int index=getTableModel().getRowCount();
  addNewTextureLayer(index);
  editorController.enableTextureButtons();
}
