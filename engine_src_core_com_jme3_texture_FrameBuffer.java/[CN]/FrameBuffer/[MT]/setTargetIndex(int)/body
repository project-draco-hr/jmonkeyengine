{
  if (index < 0 || index >= 16)   throw new IllegalArgumentException();
  if (colorBufs.size() >= index)   throw new IndexOutOfBoundsException("The target at " + index + " is not set!");
  colorBufIndex=index;
}
