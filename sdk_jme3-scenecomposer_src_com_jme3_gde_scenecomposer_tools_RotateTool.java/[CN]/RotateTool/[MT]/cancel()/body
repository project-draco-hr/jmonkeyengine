{
  if (wasDragging) {
    wasDragging=false;
    toolController.getSelectedSpatial().setLocalRotation(startRotate);
    setDefaultAxisMarkerColors();
    pickedMarker=null;
    pickManager.reset();
  }
}
