{
  Map<String,String> map=getMappings(joystickName.trim(),false);
  if (map == null)   return componentName;
  if (!map.containsKey(componentName))   return componentName;
  return map.get(componentName);
}
