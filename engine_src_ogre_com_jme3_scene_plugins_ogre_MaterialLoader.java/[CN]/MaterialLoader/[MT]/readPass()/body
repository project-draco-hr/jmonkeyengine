{
  scan.next();
  String name;
  if (scan.hasNext("\\{")) {
    name=null;
  }
 else {
    name=readString("\\{");
  }
  scan.next();
  readTexUnit=false;
  while (!scan.hasNext("\\}")) {
    readPassStatement();
  }
  scan.next();
}
