{
  scan.next();
  String name;
  if (scan.hasNext("\\{")) {
    name=null;
  }
 else {
    name=readString("\\{");
  }
  scan.next();
  while (!scan.hasNext("\\}")) {
    readPass();
  }
  scan.next();
}
