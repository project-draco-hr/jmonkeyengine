{
  SceneApplication.getApplication().enqueue(new Callable<Object>(){
    public Object call() throws Exception {
      setupOffView();
      setupOffBuffer();
      attachScene();
      return null;
    }
  }
);
}
