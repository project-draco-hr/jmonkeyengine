{
  c.setEnabled(enabled);
  for (  Component child : c.getComponents()) {
    if (child instanceof Container)     setComponentEnabled((Container)child,enabled);
 else     child.setEnabled(enabled);
  }
}
